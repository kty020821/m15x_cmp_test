"""
config/llm_config.py
════════════════════════════════════════════════════════════
분석 AI(LLM) 접속 설정

  ★ 이 파일만 고치면 된다. 다른 코드는 손대지 않는다.
  ★ 서버를 다시 시작해야 반영된다.

  ★ git 에 올리지 않는 편이 좋다 — 키가 커밋 이력에 영구히 남고,
    나중에 키를 바꿀 때 이력 정리가 번거로워진다.
    .gitignore 에 아래 한 줄을 넣어 둘 것:
        config/llm_config.py

  ★ 이 파일이 없어도 서버는 정상 기동한다.
    그때는 AI 답변 대신 계산 결과만 표시된다.
════════════════════════════════════════════════════════════
"""

# ── 접속 주소 ─────────────────────────────────────────────
#   OpenAI 호환 엔드포인트. /chat/completions 는 빼고 적는다.
#   (붙여 놓아도 코드가 알아서 떼어 내지만, 빼는 편이 명확하다)
#     예) 'https://ai.내부주소/v1'
LLM_URL = ''

# ── 인증 키 ───────────────────────────────────────────────
#   사내 게이트웨이가 키 없이 열려 있으면 비워 두면 된다.
LLM_API_KEY = ''

# ── 모델 이름 ─────────────────────────────────────────────
#   ★ 모델이 바뀌면 이 줄만 고친다.
#     사내 모델은 이름이 gpt-4o 가 아닐 수 있으니 실제 값을 확인할 것.
LLM_MODEL = ''

# ── 응답 대기 시간 (초) ───────────────────────────────────
#   도구를 여러 번 부르며 통계·머신러닝을 돌리므로 넉넉히 둔다.
LLM_TIMEOUT = 120
